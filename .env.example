# Seed Scanner 环境配置
# 复制为 .env 后修改为你的实际路径

# 扫描目标（SMB 挂载目录）
SCAN_PATH=/mnt/xspt

# 项目目录（代码、输出、数据库所在目录）
PROJECT_PATH=/opt/seed-scanner

# 时区
TZ=Asia/Shanghai

# PaddleOCR 运行时稳定性（建议 CPU 容器保持如下）
PADDLE_PDX_DISABLE_MODEL_SOURCE_CHECK=True
FLAGS_use_mkldnn=0
FLAGS_enable_pir_api=0
